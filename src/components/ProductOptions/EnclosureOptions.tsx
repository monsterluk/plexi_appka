import React from 'react';

// Dodaj komponent:
export const EnclosureOptions: React.FC<any> = ({ features, onChange }) => {
  return (
    <div>
      <h3>Enclosure Options</h3>
      {/* Tymczasowa implementacja */}
    </div>
  );
};